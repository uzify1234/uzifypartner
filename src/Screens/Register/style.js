import { StyleSheet, TextInput } from "react-native";
import { Colors } from "react-native/Libraries/NewAppScreen";
import { BACKGROUNDCOLOR, DEEPPINK, GREYISH } from "../../Constants/Colors";

export default StyleSheet.create({
    input : {
       borderWidth : 1,
       padding : 10,
       color : 'black',
       borderColor : 'black',
       borderRadius : 5,
       flex : 1,
       fontSize : 16,
       textAlign : 'left',
       letterSpacing : 1,
       fontWeight : '600',
       borderColor : 'black',
       width : '90%',
       marginTop : 10

   }
});